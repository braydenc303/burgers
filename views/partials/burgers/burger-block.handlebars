<li>
    {{burger_name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
        {{#if devoured}}Devour Burger!{{else}}Have Another!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">Plate Cleaned!</button>
</li>